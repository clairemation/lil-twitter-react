import React from 'react';
import ReactDOM from 'react-dom';
import TweetStream from './tweet-stream';
import './index.css';

ReactDOM.render(
  <TweetStream />,
  document.getElementById('tweet-stream')
);
